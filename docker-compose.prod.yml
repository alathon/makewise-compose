version: '3.4'
services:
  iam:
    restart: always
    build:
      context: .
      target: final
      args:
        - BUILD_ENV=Production

  web:
    restart: always
    build:
      context: .
      target: final
      args:
        - BUILD_ENV=Production
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
